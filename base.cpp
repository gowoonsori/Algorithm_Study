#include <bits/stdc++.h>

using namespace std;

long long solution(int n, vector<int> works);

int main() {
    int n;
    vector<int> input = {4, 3, 3};
    solution(n, input);
    return 0;
}

long long solution(int n, vector<int> works) {
    long long answer = 0;
    return answer;
}